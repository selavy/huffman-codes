#include "compress.h"
#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "heap.h"
#include "return_codes.h"

int compress(char * file_in, char * file_out) {
  printf("[Compress] in = %s ; out = %s\n", file_in, file_out);
  return SUCCESS;
}
